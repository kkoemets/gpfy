pipeline {
    agent {
        docker {
            image 'nikolaik/python-nodejs:latest'
            args '-p 3000:3000'
        }
    }

    stages {
        stage('Configuration') {
            steps {
            }
        }

        stage('Build - server') {
            steps {
            }
        }

        stage('Build - proxy') {
            steps {
            }
        }

        stage('Deploy - proxy') {
            steps {
            }
        }

        stage('Test - server') {
            steps {
            }
        }
    }
}
